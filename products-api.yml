spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ashokit_ecommerce?createDatabaseIfNotExist=true
    username: root
    password: manager
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  boot:
    admin:
      client:
        url: http://localhost:1111

management:
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: 7071
  servlet:
    context-path: /api1
